import { PlaceholderCard } from "@/components/placeholder-card";

export default function TasksPage() {
  return (
    <PlaceholderCard
      title="Tasks"
      description="A kanban-lite board (Todo / Doing / Done) with countdowns, gentle escalating reminders, and a 'first tiny step' nudge for anything that's been sitting too long. Building this in Phase 2."
    />
  );
}
