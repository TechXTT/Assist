import { PlaceholderCard } from "@/components/placeholder-card";

export default function HealthPage() {
  return (
    <PlaceholderCard
      title="Health"
      description="Light-touch tracking — exercise minutes, sleep, hydration, and a daily 1–5 mood check-in. No calorie counting, no streaks. Phase 5."
    />
  );
}
