import { PlaceholderCard } from "@/components/placeholder-card";

export default function MoneyPage() {
  return (
    <PlaceholderCard
      title="Money"
      description="Spending, budgets, bills & subscriptions, and savings goals. With an opt-in Gmail receipt scan that suggests transactions for you to confirm — never adds anything on its own. Phase 4."
    />
  );
}
